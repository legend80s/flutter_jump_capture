# 项目规则文档

## 文案规范

### 直角引号使用规范

在中文文本中，使用直角引号的层次结构如下：

1. **外层引用**：使用单引号 `「」`
2. **内层引用**：使用双引号 `『』`
3. **更深层引用**：再次使用单引号 `「」`
4. **以此类推**：根据引用层次交替使用单双引号

### 适用场景

- 任何中文内容

## 其他规范

### 代码风格
- 类名、变量、函数名等优先使用下划线前缀，即「私有优先」。好处是如果某个函数没有用到，IDE 可以提示未用到的函数。
